---
import "../app.css";
interface Props {
	title: string;
}

const { title="aolotto" } = Astro.props;
const parts = Astro.url.pathname?.split('/');
const lang = parts.shift() || parts.shift();
---

<!doctype html>
<html lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="flex h-[100vh] justify-center align-middle items-center overflow-y-scroll">
		<slot />
	</body>
</html>
<!-- <style is:global>
	@import "tailwindcss";
	html {
		font-family: system-ui, sans-serif;
		@apply bg-base-100 text-base-content;
	}

	@media (prefers-color-scheme: dark) {
		html {
			@apply bg-base-content text-base-100;
		}
	}
/* 
	.btn{
		@apply gap-2 font-semibold no-underline duration-200 ease-out px-[1em] py-[0.5em] border rounded;
		&{
			@apply text-base-content bg-base-100 border-base-100;
		}
		&:hover{
			@apply bg-base-content text-base-100 border-base-100;
		}
		&_lg{
			@apply text-2xl px-[1.2em] py-[0.8em];
		}
		&-primary{
			@apply bg-primary-content text-primary;
		}
		&-primary:hover{
			@apply text-primary-content bg-primary;
		}

	} */

</style> -->
